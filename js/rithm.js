class Rithm {
  constructor(rithm) {
    this.ctx = rithm.ctx,
    this.name = rithm.name;
    this.accents = rithm.accents;
    this.colors = {
      high:'#d29803',
      base:'#2F4D50',
      ctp: 'green'
    }
  }
}